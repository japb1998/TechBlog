<div class="container">
    {{#each comments as |comment|}}
    <div class="row">
        <div class="col-12 col-md-12">
            <h2>user.name</h2>
            <p>{{comment.comment_body}}</p>
        </div>
    </div>
    {{/each}}
</div>